This project implements the Agentic RAG to perform Semantic search in the provided documents.

Steps to make this work:

1. uv add - requirements.txt
2. Open command prompt and run 'streamlit run main.py'